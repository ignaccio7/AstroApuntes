---
import BookItem from "./BookItem.astro"
import { books } from "../data/books.js"

---
<ul class="grid grid-cols-2 md:grid-cols-3 gap-8 pt-8">
  {
    books.map(({id, title, img})=>{
      return(
        <BookItem id={id} title={title} imagen={img} />
      )
    })
  }
</ul>